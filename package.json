{
  "name": "coolstack",
  "scripts": {
    "format": "turbo run format",
    "typecheck": "turbo run typecheck",
    "build": "turbo run build",
    "dev": "turbo run --concurrency 20 db:studio dev:temporal-server dev:temporal-worker dev:trpc dev:better-auth dev:fastify dev:email dev:socket.io dev"
  },
  "devDependencies": {
    "turbo": "^2.3.1",
    "typescript": "^5.6.3"
  },
  "packageManager": "pnpm@9.0.0"
}
